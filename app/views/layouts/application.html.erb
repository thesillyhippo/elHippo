<html>
    <head>
      <title><%= title %></title>
      <%= csrf_meta_tags %>
      <%= render 'layouts/index'%>
    </head>
    <body>
        <div class="container">
            <%= render 'layouts/header'%>
            <section class="body">
                <%= yield %>
            </section>
            <%= render 'layouts/footer'%>
            <%= debug(params) if Rails.env.development? %>
        </div>
    </body>
</html>
